services:
  app:
    build:
      context: .
      args:
        VITE_APP_TOKEN: ${VITE_APP_TOKEN:-lby}
    ports:
      - "24518:24518"
    restart: unless-stopped
    init: true
    environment:
      - APP_SECRET_TOKEN=${APP_SECRET_TOKEN:-lby}
